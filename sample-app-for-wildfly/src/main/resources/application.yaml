resteasy:
  jaxrs:
    app:
      registration: property
      classes: com.sample.app.Application
management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - shutdown
  endpoint:
    shutdown:
      enabled: true
logging:
  level:
    org:
      springframework: info
